package com.ipl.auction.model;

/**
 * Enum representing the different roles a player can have.
 * Using an enum provides type safety and a fixed set of constants.
 */
public enum PlayerRole {
    BATSMAN,
    BOWLER,
    ALL_ROUNDER,
    WICKETKEEPER
}
